import { AdminExperience } from "../site-client";

export const metadata = {
  title: "Panel del propietario",
  description: "Vista de demostración del panel operativo de Rancho La Joya.",
};

export default function AdminPage() {
  return <AdminExperience />;
}
